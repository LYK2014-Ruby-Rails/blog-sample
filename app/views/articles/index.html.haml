%h1 Listing Articles

%p
  = link_to 'New article', new_article_path, class: 'btn btn-success'

%table.table.table-bordered.table-striped.table-condensed.table-hover
  %tr
    %th Title
    %th Text
    %th{:colspan => "3", width: '20%'} Options
  - @articles.each do |article|
    %tr
      %td= article.title
      %td= article.text
      %td
        = link_to 'Show', article_path(article), class: 'btn btn-sm btn-default'
        = link_to 'Edit', edit_article_path(article),  class: 'btn btn-sm btn-default'
        = link_to 'Destroy', article_path(article),
          method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-danger'
= link_to 'Home Page', root_path